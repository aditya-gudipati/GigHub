# Backend Configuration
BACKEND_URL=http://localhost:8000

# Optional: API Key for authentication (future feature)
# API_KEY=your-secret-key

# ML Model Settings
RECOMMENDER_MODE=quality  # or 'cost'
ENABLE_ML_MODEL=false     # Set to true when ML model is trained

# Database (for future MongoDB integration)
# MONGODB_URI=mongodb://localhost:27017/gighub
# MONGODB_DB=gighub
